[package]
name = "wirerust"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[features]
regex = []

[dev-dependencies]
serde_json = "1.0"
criterion = "0.5"

[[bench]]
name = "filter_bench"
harness = false
